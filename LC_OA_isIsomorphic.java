class Solution {
    public boolean isIsomorphic(String s, String t) {
        int sl = s.length();
        int tl = t.length();
        
        if (sl!=tl) return false;
        
        // get cnt of all characters for each in 2 separate hashmaps
        
        // iterate over both hashmaps and add their values to 2 stacks
        
        // pop the items off the stacks and compare until no values left
            // if any don't match, return false
        
        // otherwise return true
    }
}
