class Solution {
    public String longestCommonPrefix(String[] strs) {
        String cp = "";  // common prefix
        int l = strs.length();
        
        // get minimum length of the strings

        // iterate over, check curr, add char to result if all strings contain the char at the same places
    }
}
